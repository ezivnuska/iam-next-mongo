// app/ui/poker/player-action-area.tsx

'use client';

export default function PlayerActionArea() {
  // Winner display is now shown in GameCountdownTimer within NotificationArea
  // Player controls are now shown in player-user component
  return null;
}
